import { TabContent } from '../../../helpers/types';
import * as S from './styles';

export const TabInnerContent = ({ tabContent }: TabContent) => {
  return <S.TabInnerContent>{tabContent}</S.TabInnerContent>;
};
